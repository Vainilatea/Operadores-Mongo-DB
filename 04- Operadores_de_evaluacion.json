"""

https://regex101.com/

https://regexr.com/
"""

// $regex: Buscar por patrón
db.usuarios.find({
  "nombre": { $regex: /^A/i }   // Nombres que empiezan con "A", sin distinguir mayúsculas
})

// $expr: Comparar campos dentro del mismo documento
db.ventas.find({
  $expr: { $gt: ["$precioFinal", "$precioBase"] }
})
